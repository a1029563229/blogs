#include <stdio.h>

/* when fahr = 0, 20, ..., 300, 
print fahr and celsius compare table */
int main() {
  int fahr, celsius;
  int lower, upper, step;

  lower = 0; /* lower limit of thermometer */
  upper = 300; /* upper limit of thermometer */
  step = 20; /* step */

  fahr = lower;
  while(fahr <= upper) {

  }
}